# Product Access

## Principle
A buyer can access a product only after the related order is verified as paid.

## Flow
`PAID → Create Product Access → My Products → Access Product`

The raw external delivery URL must not be shown publicly on the product page.

For the MVP, access can redirect to the seller's external storage URL after server-side authorization.
